export type profileProps =
  | {
      uid: number;
      gi: string;
      so: string;
      bio: string;
      email: string;
      username: string;
    }
  | undefined;

export type profilePageProps = {
  shk: string;
};
